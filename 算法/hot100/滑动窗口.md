3.无重复字符的最长子串
- 因为牵扯到字符判断,所以要把字符串转成CharArray,创建一个数组用来计数,每次进入循环,当前元素计数加一,当当前元素计数大于1,滑动本次窗口,将计数减1,再把左端点右移一位,因为题目要求最长,所以每次循环都要比较最大值,Math.max(ans, right - left + 1)

438.找到字符串中所有字母异位词
- 先对目标字符串进行计数,计算每个字母的出现次数,然后对源字符串进行计数,进入循环先判断窗口长度是否达标,不达标则continue,记left为起始索引,left = i - p.length() + 1,如果这两个计数数组Arrays.equals()相等了,那么把left加入答案,否则将左端点计数减1,进入下一个窗口

